{
  "server": {
    "host": "localhost",
    "port": 3000
  },
  "register": {
    "plugins": [
      {
        "plugin": "./plugins/category",
        "routes": {
          "prefix": "/category"
        }
      },
      {
        "plugin": "./plugins/transaction",
        "routes": {
          "prefix": "/transaction"
        }
      }
    ]
  }
}